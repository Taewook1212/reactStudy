//createElement 함수
//Dom 노드 : HTMLElement 반환
function createElement(type, props, ...children) {}
// createRoot 함수
// 객체 : { render, unmount }

function createRoot(domNode /*contaniner */) {}

// EUID 객체의 멤버로 내보내기
